# Joy4_sub - alias for direct runtime dependencies.
#
# The actual setup script (Joy4_sub-Setup.bat) uses requirements-venv.txt.
# This file mirrors it so that `pip install -r requirements.txt` also works.
#
# For CUDA 12.1 PyTorch wheels, install with:
#   pip install --extra-index-url https://download.pytorch.org/whl/cu128 -r requirements.txt

-r requirements-venv.txt
