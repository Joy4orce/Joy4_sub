# Joy4_sub - direct runtime dependencies
# Used by Joy4_sub-Setup.bat to provision a working .venv-vatsg
# Install with: pip install --extra-index-url https://download.pytorch.org/whl/cu128 -r requirements-venv.txt
# CUDA 12.8 builds are required for NVIDIA Blackwell GPUs (RTX 50-series, sm_120).
# Older cu121 builds only cover up to sm_90 and fail on a 5080 with
# "no kernel image is available for execution on the device".

# --- PyTorch (CUDA 12.8 builds — required for RTX 50-series / Blackwell) ---
torch==2.11.0+cu128
torchaudio==2.11.0+cu128
torchvision==0.26.0+cu128

# --- Speech-to-text ---
faster-whisper==1.2.1
ctranslate2==4.7.1
stable-ts==2.19.1
openai-whisper==20250625

# --- Translation engines ---
deepl==1.16.1
google-genai==1.73.1
openai==2.32.0

# --- Media handling ---
moviepy==2.2.1
mutagen==1.47.0
imageio-ffmpeg==0.6.0

# --- UI / OS ---
tkinterdnd2==0.4.3
qrcode==8.2
pywin32==306

# --- Misc ---
setuptools==80.9.0
